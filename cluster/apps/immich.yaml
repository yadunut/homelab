apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: immich
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure
  interval: 10m0s
  path: ./cluster/apps/immich
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
