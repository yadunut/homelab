apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: litellm-tls
  namespace: litellm
spec:
  secretName: litellm-tls
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
  dnsNames:
    - llm.yadunut.dev
