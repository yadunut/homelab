apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hermes-tls
  namespace: hermes
spec:
  secretName: hermes-tls
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
  dnsNames:
    - hermes.yadunut.dev
