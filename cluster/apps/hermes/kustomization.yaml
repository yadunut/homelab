apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - onepassworditem.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingressroute.yaml
configMapGenerator:
  - name: hermes-bootstrap
    namespace: hermes
    files:
      - config.yaml=bootstrap-config.yaml
