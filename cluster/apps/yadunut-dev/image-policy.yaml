apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: yadunut-dev
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: yadunut-dev
  digestReflectionPolicy: Always
  interval: 1m0s
  filterTags:
    pattern: "^main$"
  policy:
    alphabetical: {}
