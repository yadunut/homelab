apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: forgejo-nbconvert
  namespace: flux-system
spec:
  image: harbor.yadunut.dev/yadunut/forgejo-nbconvert
  interval: 1h
  secretRef:
    name: flux-harbor
