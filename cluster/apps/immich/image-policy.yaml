apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: immich
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: immich
  filterTags:
    pattern: "^v(?P<version>\\d+\\.\\d+\\.\\d+)$"
    extract: "$version"
  policy:
    semver:
      range: ">=3.0.0 <4.0.0"
